# Session Handoff v39 — 门户 v2＋README＋远端（2026-09-16）

门禁：pytest 84 / numbers-gate PASS / v2 三语 117/117 / NAV=DOM / PDF 三处同哈希。

## 门户 v2（推倒重写）

- `index.v2.html` 643 行/58KB（v1 2635 行/252KB 归档为 `index.v1-20260916.html`）；
  动线：hero（30s 三数＋三态徽章）→ validation 前置 → findings A–E（Two-Tier 并入 C、探针并入 E 折叠块）
  → method → limitations → sources（折叠）→ curriculum → 3D → paper → open science；
  NAV 与 DOM 一一对应；data-i18n 全覆盖 117 键（node 烟测三语全通）；数字全 SSOT。
- `_deploy/index.html` 已重镜像；v1 旧 `_deploy/portal/` 快照声明作废（待并行组 deploy 流程重建）。

## README（逐个改）

- final：F12 双行合并（含 177 caveat）、55→59 八方构成、W 注记 committed；
  docs/submission：同 59 构成＋W 注记同步；pitch：55/50→59/54；
  portal：55-margin→59；cognitive-space：574→556、教材 39/18/11（实数）；
  根 EN：补工具分层（与 ZH/DE 对齐）。

## 远端

- Latest：v0.4 → `data-weight-vectors-20260916`；v0.4 notes 追加 tag 命名说明。
- 未 push 代码、未碰分支（H4 维持）。

## 修补

- numbers-gate：v2 措辞升级＋ascii-safe 输出（gbk 终端崩溃修复）。
