# E3 执行报告：238边 edge-count-matched-only J_edge-only(gid,edge-count-matched-only)（2026-09-16，A线，fix_rev 2026-09-16）

> **状态：E3 测量 EXECUTED；口径 Hypothesis/PENDING；解释 Hypothesis/PENDING。**本文件所有新统计均为 **exploratory appendix-only**，不得进入主结论。
> **边数对齐置顶声明（R-E3-1 fix）：仅边数对齐 238/语，节点数（199 vs 556）与密度（0.012081 vs 0.001543，约 7.8×）+ 平均度（2.3920 vs 0.8561，约 2.8×）仍 MISMATCH；跨密度 Jaccard 混杂持续，解释维持 MISMATCH/Hypothesis 封顶。本报告禁用退役措辞（稀疏·匹配/可比/稀疏度对齐），一律记 edge-count-matched-only。**
> **红线核验**：只读 `data/` 既有与既有向量文件；未改 `tests/`、`freeze/`、`_deploy/`、`linguaGraph.db`、`data/` 既有；未改 `scripts/tools/weight_graph_audit.py`、`scripts/tools/weight_graph_E1b.py` 及其输出；`git status` 无 M/D 改动（仅新增本报告 + `research/weight_graph_E3_20260916.json` + `scripts/tools/weight_graph_E3.py`）；新增概念 **0**；未扩语料；未改 LDS v3 公式（冻结公式 `1-(J_node+J_edge)/2`，`docs/lds_formal_definition.md §8.1` 逐字复用，与 audit/E1b 脚本同一实现）。
> EN 上限 / ZH-DE 采信规则（沿用 E1/E1b §红蓝）：涉 EN 断言停在分布级；zh-de（主对 n=65）永不单独承载结论，三语对并列。人类 pilot N=8：禁一切显著/通过/报警断言。
> **输入 hash 声明（继承 E1b-A7）**：本报告输入（`aligned_data.json` 219 行、`weight_vectors_934x768_20260916.term2vec.json` 934×768）**无 hash 落盘**，字节一致性未验证；复现仅靠行数/行序人工对账。
> **指标命名声明（继承 E1b-A1，R-E3-1 改名）**：本文件指标为 **`J_edge-only(gid,edge-count-matched-only)`**（原 `J_edge-only(gid,238matched)` 改名，数值/公式不动）。因三语 gid 集完全相同（J_node=1.0 系构造），冻结公式退化为 **`J_edge-only=(1-J_edge)/2`**，纯系边重合度的单调变换；**值域 [0,0.5] 不对称**，与 LDS-K 跨量纲、禁一切数值互证（正反两方向皆禁，双向维持 R-E3-9）。该指标为 E1b 新造、未经独立验证，E3 沿用其嫌疑身份，**嫌疑声明**：不得视为 LDS 验证，不得进主结论；rank 对照仅为附录未验证方法笔记，正文禁比禁引用。
> **口径继承声明（R-E3-5 强调，零修复）**：翻译层 term→gid 沿用 E1b 首行规则（R-intra/R-gid/R-cross/R-loop 不变），并**全额继承其偏倚声明，零修复**：MNAR 缺失 2/201（`math_linear_algebra_基`、`math_linear_algebra_维数`）、18 处同 gid 首行取舍即选择偏倚（2 处单标签变体：`math_calculus_洛必达法则` de 槽、`math_calculus_散度` en 槽；无末行重算，禁一切“无损/干净”措辞）、跨 gid 同标签首-gid 映射 18 条（节点不合并）、J_node 去污而 **J_edge 仍污染**（de 槽 CJK 残留 + 孪生对机制点名，见 §3）。E3 不新增口径修复，R5 维持 Hypothesis/PENDING。
> **复现声明（R-E3-6 降级）**：本轮不用退役措辞（完全·可复现），仅记**同机同seed确定性**（`numpy.random.default_rng(seed)` 给定种子同机确定；numpy/python 版本未锁定、模型 revision 未知，跨机/跨版本不保证一致）。

## §1 E3 方法（冻结，R-E3-1/6/8 改名+声明）

- 输入（只读，无 hash）：`research/weight_vectors_934x768_20260916.term2vec.json`（934×768，`text-embedding-nomic-embed-text-v1.5`，模型 revision 未知，LM Studio `/v1/models` 未暴露，沿 E1）+ `data/math_extractions/merged/aligned_data.json`（219 行 = 201 唯一 gid + 18 同 gid 重复行）+ 人类基线 `manifest.json`（556 节点 / 238 边 / 密度 0.001543 / 平均度 0.8561）。
- 执行：`python scripts/tools/weight_graph_E3.py`（新文件，E1/E1b 脚本未动）→ `research/weight_graph_E3_20260916.json`（机器输出）。
- 候选池：逐语对称 kNN（k=5/10/15，余弦距离，ties 按文件序稳定排序，与 E1/E1b 同规则），节点为 199 gid 集；每条候选无向边落权重表：`cosine_distance d=1-sim`、`cosine_similarity sim`、采样权重 **`w=(sim+1)/2 ∈ [0,1]`**（随接近度单调、恒非负；落盘 6 位小数；实测窄带约 0.71–1.0，见 §6）。
- 边数对齐抽样（原稀疏·匹配/weight措辞已退役，改名，数值不动）：每语每 k 池按权重 subsample **238 无向边**。方法二选一如实记录——本次用 **`near-uniform-single-draw`（原 weight 改名：不放回加权随机抽样，`p(e)=w(e)/Σw`，`numpy.random.default_rng(seed)`；因 w 窄带 0.71–1.0，接近均匀抽样，故改名；单次抽样，无 CI；`threshold` 即 top-238 按距离截断本次未用，其对照列 PENDING；零距离边剔除对照列 PENDING）**。基种子 **`BASE_SEED=20260916`**，派生规则 `seed(k,lang)=20260916+k*100+lang_index`（zh=0,en=1,de=2），九组种子见 §6（k=5：20261416/17/18；k=10：20261916/17/18；k=15：20262416/17/18）。同机同seed确定性；单次抽样，无多重运行，无 CI；≥200 次重复稳定性列 PENDING。
- 图比较：边端点按 **gid 对**比较，节点集 identical → J_node=1.0 系构造；重算 **`J_edge-only(gid,edge-count-matched-only)`**（冻结公式退化，新指标嫌疑，未验证）。
- 方法可信锚限域：E3 与 E1b 共享 kNN 代码路径（同稳定排序、同余弦规则），候选池边数逐值复现 E1b（k=5：768/719/763；k=10：1512/1370/1474；k=15：2249/2009/2197）；该复现**仅代码路径一致性冒烟，gid 新路径与 238 抽样均无独立验证**。

## §2 E3 结果（只记数值，序不可解释；rank 明细移附录，R-E3-3/4/7/9）

| k池 | zh-de J_edge-only(238) | zh-en J_edge-only(238) | de-en J_edge-only(238) | 序（升序，仅记录，禁解释） |
|---|---|---|---|---|
| 5 | 0.4400（J_node=1.0，J_edge=0.1200，交51/并425） | 0.4826（1.0 / 0.0348，交16/并460） | 0.4746（1.0 / 0.0508，交23/并453） | 数字罗列，序不可解释 |
| 10 | 0.4746（1.0 / 0.0508，交23/并453） | 0.4915（1.0 / 0.0171，交8/并468） | 0.4904（1.0 / 0.0193，交9/并467） | 数字罗列，序不可解释 |
| 15 | 0.4803（1.0 / 0.0393，交18/并458） | 0.4947（1.0 / 0.0106，交5/并471） | 0.4947（1.0 / 0.0106，交5/并471） | 数字罗列，序不可解释（含 4 位并列） |

- tie-aware 声明（R-E3-7）：并列视为不等；任何 4 位小数并列对之间禁止序差断言（T-P5-3 类比：数值接近/并列≠可排序）。k=15 zh-en/de-en 在 4 位并列，故跨 k 方向叙事（退役措辞一律不用）一律删除，不记任何方向性叙事。
- **J_edge 地板声明（R-E3-4）**：J_edge 实测 0.0106–0.1200，近零；随机底线（null/随机图对照）未建，无从判断“高于随机/低于随机”，禁一切“信号/结构/对齐”措辞。
- **禁比数值声明（R-E3-9 双向维持）**：不许说“向量验证/证实了 LDS-K 数值”，亦不许说“0.44–0.49 证伪了 0.519/0.93”。J_node=1.0 系构造使然（同 199 gid 集），J_edge-only=(1−J_edge)/2 值域 [0,0.5] 不对称，纯系边重合度的单调变换，与 LDS-K 跨量纲、禁一切数值互证句（正反两方向皆禁）。新指标嫌疑声明见文件头。冻结 LDS-K 数值不在正文对照，仅附录方法笔记记账（§7）。
- 与 E1b 同 k 值的任何差值比较（E3-238匹配 vs E1b-全kNN）仅记账为“池内抽样后果”，不记因果、不记来源归因（单次加权抽样 + 密度变更混杂，不可分解）。
- rank 对照正文仅一句话（R-E3-3/4/9）：**n=3 无判别力，序不可解释；明细移 §7 附录未验证方法笔记，正文禁引用；rank 系数禁进 headline/摘要。**

## §3 口径复核（继承 E1b 199/201 有偏口径，Hypothesis/PENDING，R-E3-5 强调零修复）

- 行普查（继承，不重算）：对齐表 **219 行 = 201 唯一 gid + 18 同 gid 重复行**；18 处取首行即选择偏倚，2 处单标签变体点名（`math_calculus_洛必达法则` 行12/39 de 槽、`math_calculus_散度` 行21/61 en 槽）；无末行重算前，**禁一切“无损/干净”措辞**。
- 缺失 2 组（MNAR，非随机缺失；缺失率 gid 口径 2/201=1.0%）：`math_linear_algebra_基`、`math_linear_algebra_维数`（三语表面全同、从未进入 434 主对增补集，934 词表无其向量——属输入口径后果，非本层可修复；零新文本口径下禁补）。
- 向量覆盖：每语 217 行有向量；折叠后每语 **199 唯一 gid**有向量，三语 gid 集完全相同 → J_node=1.0 系构造（非测量）。
- 去污状态声明（继承，零修复）：**J_node 去污、J_edge 仍污染**。机制点名：(1) de 槽 CJK 残留（de cross_gid 重复中 CJK 表面存于 de 槽，首-gid 映射改变近邻）；(2) 孪生对机制（18 重复行折叠 + 跨 gid 同标签首-gid 映射 + 行空间自环/零距离近邻结构渗入边集；本轮候选池三语 `dist_min=0.0` 即零距离孪生对实证；零距离剔除对照 PENDING）。故禁一切“剥离/去污染/干净”措辞。
- 自环：gid 空间三 k 三语自环均为 0（构造性为 0，非测量成功；E1 行空间基线自环数见 E1b §3，E3 未重算）。

## §4 边数对齐上下文（边数已对齐，密度/平均度仍 MISMATCH，R-E3-1；退役措辞不用）

- 人类教材图基线：556 节点 / 238 边 / 密度 0.001543 / 平均度 0.8561。
- E3 边数对齐图：每语 199 节点 / 238 边 / 密度 0.012081 / 平均度 2.3920——**边数已对齐 238，但节点数（199 vs 556）与密度（0.012081 vs 0.001543，约 7.8 倍）+ 平均度（2.3920 vs 0.8561，约 2.8 倍）仍失配**：跨密度 Jaccard 混杂持续，解释维持 MISMATCH/Hypothesis 封顶。密度 typo 已按 JSON/重算对齐为 0.012081（旧值已作废，见 §8）。
- 候选池记账（落盘权重表规模）：k=5 池 719–768 边、k=10 池 1370–1512 边、k=15 池 2009–2249 边（与 E1b 逐值一致）；238 抽样率约 31% / 16% / 11%。

## §5 阶段门禁自评（测量 EXECUTED、口径与解释均 Hypothesis/PENDING）

| 项 | 裁决 | 说明 |
|---|---|---|
| E3 测量 | **EXECUTED** | 全量 gid-kNN 候选边权重表落盘 + near-uniform-single-draw 法 238 边 subsample（三 k 池，同机同seed确定性；numpy/python 未锁定，模型 revision 未知）+ J_edge-only(238) 重算 + 附录 rank 方法笔记；输入无 hash；单次抽样，无 CI，≥200 次重复 PENDING |
| R5 节点口径 | **Hypothesis/PENDING** | 全额继承 E1b 199/201 有偏口径（MNAR 2/201 + 首行选择偏倚未排除 + 跨 gid 映射 18 条 + J_edge 仍污染），零修复；E3 不新增口径修复；待末行重算 + 零距离剔除对照 |
| E3 解释 | **Hypothesis/PENDING（维持）** | n=3 无判别力（序不可解释；p 明细见附录 §7，禁进正文）+ k=15 EN 并列禁序差（tie-aware，并列视为不等）+ 密度仍 7.8 倍 + 平均度仍 2.8 倍失配 + zh-de n=65 降权三重封顶；rank 与 k-敏感性均待独立复核 |
| 升 Developing | **否** | 唯一升级路径为独立复核（抽样稳定性 + 末行重算 + 密度失配 adjudication）；单次加权抽样 + 嫌疑指标不可升级 |
| E2 / 分支1/2 | **PENDING（不变）** | 无冻结数学频次表 / 无语义去污判官 |

## §6 输出清单与权重表摘要/seed/k-敏感性记账（R-E3-2/7/8）

- `research/weight_graph_E3_20260916.json`（机器输出：候选权重全表/三 k 238 边集/附录 rank 方法笔记/边数对齐/门禁自评，`exploratory_appendix_only=true`）
- 本报告（E3 唯一新建叙事文件）
- `scripts/tools/weight_graph_E3.py`（新脚本；audit/E1b 脚本与其输出未动）
- 权重表摘要（w=(sim+1)/2，d=1−sim；6 位小数落盘；窄带约 0.71–1.0，故 weight 改名 near-uniform-single-draw）：

| k池 | 语 | 候选n | w均值 | w范围 | d均值 | d范围 |
|---|---|---|---|---|---|---|
| 5 | zh | 768 | 0.951236 | 0.752099–1.0 | 0.097529 | 0.0–0.495801 |
| 5 | en | 719 | 0.828605 | 0.727016–1.0 | 0.342790 | 0.0–0.545968 |
| 5 | de | 763 | 0.919291 | 0.731547–1.0 | 0.161418 | 0.0–0.536907 |
| 10 | zh | 1512 | 0.939197 | 0.742384–1.0 | 0.121607 | 0.0–0.515232 |
| 10 | en | 1370 | 0.808253 | 0.719581–1.0 | 0.383493 | 0.0–0.560839 |
| 10 | de | 1474 | 0.904437 | 0.726294–1.0 | 0.191126 | 0.0–0.547412 |
| 15 | zh | 2249 | 0.931721 | 0.742384–1.0 | 0.136558 | 0.0–0.515232 |
| 15 | en | 2009 | 0.796288 | 0.717154–1.0 | 0.407424 | 0.0–0.565692 |
| 15 | de | 2197 | 0.896624 | 0.722087–1.0 | 0.206752 | 0.0–0.555827 |

- seed（方法 near-uniform-single-draw，`seed=20260916+k*100+lang_index`，`numpy.random.default_rng`，不放回；同机同seed确定性，numpy/python 未锁定）：k=5 zh/en/de = 20261416/20261417/20261418；k=10 = 20261916/20261917/20261918；k=15 = 20262416/20262417/20262418。picked 权重均值高于池均值（加权抽样后果，记账不解释）。threshold 对照 PENDING；零距离剔除对照 PENDING。
- k-敏感性记账（R-E3-2/7：记账完成、解释待定）：三对各 k 取值见 §2 表（极差不作结论句，数值记账以 JSON 为准）；单次抽样下 k 效应与抽样噪声不可分解，无 CI；≥200 次重复稳定性 PENDING；跨 k 方向叙事一律删除（tie-aware：k=15 EN 并列视为不等，故退役标签不再使用）。**k-敏感性记账完成、解释待定。**

## §7 附录A：未验证方法笔记 rank 对照（禁引用，R-E3-3/4/9）

> 本附录为未验证方法笔记，正文禁引用；ρ 禁进 headline/摘要；数值仅记账，序不可解释。

- 冻结 LDS-K 序（升序，仅记账）：zh-de（0.519）< zh-en（0.934）< de-en（0.938）。
- k=5：J 序 zh-de < de-en < zh-en，Spearman ρ=0.5（n=3，描述性；精确双侧置换 p=1.0；n=3 无力，序不可解释）；4 位无并列。
- k=10：J 序 zh-de < de-en < zh-en，Spearman ρ=0.5（n=3，描述性；精确双侧置换 p=1.0；n=3 无力，序不可解释）；4 位无并列。
- k=15：J 4 位值 zh-de 0.4803 / zh-en 0.4947 / de-en 0.4947，Spearman ρ=0.866（midrank artefact：EN 并列以 midrank 计得，并列视为不等，禁任何 EN 序差断言；精确双侧置换 p=0.6667；n=3 无力，序不可解释）。
- 三 k“序一致”不充数，不记任何方向性叙事；zh-de 数值最小仅记账，不记支持/印证。

## §8 fix_rev 2026-09-16 修复对照（数值不动）

| 红队项 | 修复 |
|---|---|
| R-E3-1 改名+置顶 | 标题/方法/JSON 标签退役措辞 → `edge-count-matched-only/边数对齐`；删“可比”暗示；密度 7.8× + 平均度 2.8× 置顶；维持 MISMATCH/Hypothesis |
| R-E3-2 k 敏感性 | 删极差结论句与方向叙事，只留“记账完成、解释待定”；声明单次抽样无 CI；≥200 次重复列 PENDING |
| R-E3-3 ρ | ρ 禁进 headline/摘要（仅附录 §7）；捆绑 n=3 无力 p 值，只留“序不可解释”；k15 并列注 midrank artefact |
| R-E3-4 地板+降级 | 补 J_edge 近零（0.0106–0.1200）+ 随机底线未建声明；rank 表降附录未验证方法笔记，正文禁引用 |
| R-E3-5 三偏倚 | MNAR/首行/J_edge 污染零修复强调；R5 维持 Hypothesis/PENDING |
| R-E3-6 复现 | 退役措辞→“同机同seed确定性”；模型 revision 未知 + numpy/python 未锁定声明 |
| R-E3-7 方向叙事 | 删方向叙事（退役措辞一律不用）；tie-aware 声明（并列视为不等），退役标签不再使用 |
| R-E3-8 weight 法 | weight → near-uniform-single-draw（窄带 w 约 0.71–1.0）；threshold 对照 + 零距离剔除列 PENDING |
| R-E3-9 附录化 | rank 块移 §7 附录“未验证方法笔记”；正文禁比维持双向 |
| Typo | md 密度旧值已作废→0.012081，对齐 JSON/重算；平均度 2.3920、人类平均度 0.8561 补齐 |

**最终 verdict：E3 测量 EXECUTED；R5 口径与 E3 解释维持 Hypothesis/PENDING；不升 Developing；rank/k-敏感性待独立复核（≥200 次重复 + 末行重算 + 密度失配 adjudication + threshold/零距离对照）。**
