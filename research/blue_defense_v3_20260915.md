# 蓝队防守书 v3（2026-09-15，终版数）

- 立场：蓝队（防守方）。只为"口径可复现、缺席处理保守、分层结论可用"辩护；核验缺口全部记债务。不洗白。
- 已读 V 依据：`research/consensus_v3_20260915.md` 全文（口径 :3-5；覆盖表 :9-17；intra :23-31；inter 矩阵 :37-45；vs_mimo :49-57；表决 :61-89）。落盘数（72/72 等）系 run 通报，记 A。
- 证据分级：**V=行号可查 / A=断言方向**。A 不进可登记口径。

## ① 防守证据

**B1. 口径冻结未变，0.5401 是补分母重算值【V】**：归一化/Jaccard/代表 run/缺席不计分（`:3-5`）v2→v3 一字未改；矩阵均值 0.5401（`:45`）；qwen–kimi 共存 n=24（`:39-43`），ds 相关 n=4/5/18。只许报"补齐共存后的重算值"，不许报"方法改进"。

**B2. qwen 四模型 n=24 齐整 intra 0.59–0.63 + kimi n=24/0.7291【V】**：`:23-31` 实测（27b 0.5862 / 3.7 0.6035 / max 0.6276 / max0902 0.6304 / kimi 0.7291，n=24；v41 n=14 / pro n=5 只许"小-n 初值"）。自一致可用性的硬度在 n 齐整，不在最高值。

**B3. 缺席不计分是保守口【V】**：`:5` 缺席不计分；`:11-12` pro 缺席 19、v41 缺席 6；kimi/qwen 零缺席（`:13-17`）；bailian 53 全缺席（`:19`）。missing 不产生 0 分摊薄——对己方不利的保守口径。

**B4. 审计门【V 规则 + A 实例】**：规则（15–40 概念/10–30 关系/引用闭合/无 `?�` 乱码/fresh-stage 守卫）在 `scripts/tools/ensemble_refill_one.py` 可查【V】；本轮复本污染拦截实例与 90 文件审计 log 未独立成文，记 A——"审计闭环 verified"禁报，实例入库前只许报规则存在。

**B5. ds/v41 降权的正当性【V 计算层 + A 根因层】**：vs_mimo 全系 0.22–0.26（`:49-57`，最高 3.7-flash 0.2583），纳入与否 headline 不变；inter 行 ds-pro 0.5819/v41 0.5739 居高位，拿掉二者均值更保守——排除是压低分母保可比。根因实证（16k 截断/32k 超时/v41 止损 log）记 A，入库前禁报"根因已定"；gold/harness 层债务继承 v2 D1。

**B6. 表决是严门【V】**：thr=n//2+1（`:59`）；收录 540 / Solid 194 / Weak 736 / Hypo 1716 / mimo 独有候选删 73（`:61`）；分文件源/thr 可查（`:65-88`）。Solid/收录 ≈36%，mimo 无特权。只许报"严格多数门下的分层数"，禁报"540 即定稿"。

## ② 承认的债务（红队可直接引用）

1. 审计实例 + pytest log 未独立入库（产物债）——禁报 verified。
2. ds/v41 根因实证未入库（实证债）——禁报根因已定。
3. depth 全量三数（72/72、71+1、71+1 缺席确认）manifest 行号待归档（覆盖债）——"全教材覆盖"禁报；深度 24 不得外推全量。
4. bailian 53 缺席 + vs_mimo 低重合归因未冻结（解释债）——只许"低重合快照"，禁报谁对谁错。
5. thr 浮动敏感性表未出（方法债，红 A3）——总数须并列 thr 分布 + Solid=0 文件单列。

## ③ 可登记口径与禁语

- 允许报："归一化小写+去空白…qwen 四模型 intra n=24 全勤 0.59–0.63（kimi n=24/0.7291）；inter 0.5401（补齐重算值，v2 0.4629 不可比）；vs_mimo 0.22–0.26；严格多数下 540/194/736/1710/73。"
- 允许报："深度 24 内 kimi/qwen 零缺席；pro 缺席 19、v41 缺席 6、bailian 53 全缺席，缺席不计分；ds/v41 降权保可比，vs_mimo 不变；harness 债继承 v2。"
- 允许报（分层继承）："宏观共识与微观噪声不在同一检验对象；互不反证。"
- 禁语："方法改进致 inter 提升""审计已闭环 verified""pytest 已验证内容""ds/v41 根因已定""540 即定稿""全教材已覆盖""vs_mimo 低即模型错""缺席计 0"。
- 转正：审计实例 + 根因 log + 全量 manifest 三数 + thr 敏感性表 + mimo 归因冻结，五者入库方转 V。
